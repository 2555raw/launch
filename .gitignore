node_modules/

# See hydropad/.gitignore: screenshots are throwaway, artwork under media/ ships.
e2e-*.png
*.png
!**/media/**/*.png
# Brand artwork ships too: the headers and the marks are deliverables, not
# screenshots. Without this line they live only on whatever machine made
# them, which is how a logo goes missing.
!**/brand/**/*.png
