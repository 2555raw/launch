Drop official logos here as `<ticker>.png` (lowercase), e.g. `tsla.png`, `nvda.png`,
`aapl.png`, `googl.png`, `hood.png`, `pons.png`. Square images work best; they are
cropped to a circle. When a file is present the page draws it over the ticker text.
Only add logos you have the right to use (most companies publish a media/brand kit).
