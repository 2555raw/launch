{
  "name": "hanamy",
  "version": "1.0.0",
  "private": true,
  "description": "Every model, one look. A shared inference pool behind one key.",
  "scripts": {
    "start": "node server.js",
    "build": "python3 build.py",
    "serve": "node server.js"
  },
  "engines": { "node": ">=18" }
}
