(() => { seekr.nav(); seekr.api('/api/config').then((c) => { document.getElementById('xCard').href = c.links.x; document.querySelector('#xCard h4').textContent = '@' + c.links.x.split('/').filter(Boolean).pop(); }).catch(() => null); })();
