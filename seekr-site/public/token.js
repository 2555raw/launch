(() => { const { api, usd, pct } = seekr; seekr.nav(); api('/api/markets').then(({ seekr: a }) => { document.getElementById('tPrice').textContent = a.live ? usd(a.price, 6) : 'At launch'; const c = document.getElementById('tChange'); c.textContent = a.live ? pct(a.change) : 'Soon'; c.className = 'v ' + (a.change >= 0 ? 'chg-up' : 'chg-down'); }).catch(() => null); })();
