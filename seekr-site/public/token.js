(() => { const { api, usd, pct } = seekr; seekr.nav(); api('/api/markets').then(({ seekr: a }) => { document.getElementById('tPrice').textContent = usd(a.price, 6); const c = document.getElementById('tChange'); c.textContent = pct(a.change); c.className = 'v ' + (a.change >= 0 ? 'chg-up' : 'chg-down'); }).catch(() => null); })();
